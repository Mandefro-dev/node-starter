{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "Generated by nodex CLI",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "build": "tsc"<% if (tools.includes("jest")) { %>,
    "test": "jest"<% } %><% if (tools.includes("lint")) { %>,
    "lint": "eslint src/**/*.ts"<% } %>
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"<% if (dbType === 'MongoDB') { %>,
    "mongoose": "^7.0.0"<% } %><% if (dbType === 'PostgreSQL') { %>,
    "pg": "^8.10.0",
    "sequelize": "^6.31.0"<% } %>
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "@types/node": "^18.15.0",
    "@types/express": "^4.17.17"
  }
}